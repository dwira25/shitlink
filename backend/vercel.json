{
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "runtime": "nodejs22.x",
        "maxDuration": 30
      }
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
